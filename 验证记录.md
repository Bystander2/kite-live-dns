# 本地交付与验证记录

日期：2026-09-17。当前决定：选择 Cloudflare Workers 作为目标托管平台；尚未部署、付费或提交 PR。

## 已完成

- `GET /v1/dns?name=example.com&type=A`，支持 A、AAAA、MX、TXT、NS、CNAME、SOA、CAA。
- 固定 HTTPS 上游 Google Public DNS；禁用重定向、6 秒超时、不转发客户端凭据。
- 官方 x402 Express 中间件：验证付款 → 查询 → 成功后结算。
- 官方 `kite.ts` 原样保留；默认测试网，收款地址必须由用户配置。
- 参数校验拒绝 URL 路径、查询串、片段等；支持 IDN、DMARC/DKIM 下划线标签。
- Dockerfile、GitHub Actions 工作流、清单、README、锁文件和测试。

## 已验证的证据

| 检查 | 结果 |
|---|---|
| TypeScript 类型检查 | 通过 |
| 自动测试 | 15 项通过 |
| 生产构建 | 通过 |
| 官方 JSON Schema 清单校验 | 通过，draft 地址为零地址占位 |
| 官方 facilitator 真实未付费请求 | 返回 402，网络 eip155:2368 |
| 真实 HTTPS DNS 查询 | 成功返回公网 A 记录 |
| 官方 kite.ts 一致性 | 与模板逐字一致 |
| npm 安装时依赖审计 | 报告 0 个已知漏洞；不等于完整安全审计 |

`evidence/live-unpaid-smoke.json` 保存真实未付费请求与独立 DNS 查询结果。
其中 0x111…111 是测试用占位地址，不属于用户，绝不可向它付款。
自动测试使用本地模拟 facilitator，不能用来证明链上签名或结算成功。

## 运行

```sh
cd services/live-dns
npm ci
npm run build
cp .env.example .env
# 在 .env 填写自己的公开收款地址 PAY_TO；不需要私钥。
node --env-file=.env dist/index.js
```

```sh
curl -i 'http://localhost:8080/v1/dns?name=example.com&type=A'
```

预期返回 402。未配置真实公开收款地址时，可运行独立 smoke 检查：

```sh
npm run build
node scripts/smoke.mjs
```

此命令仅请求 402 和独立查询 DNS，不进行付款。它会更新证据文件。

## 尚未完成，不应宣称已验收

- 公网部署、真实收款地址、本人链上付费调用。
- Docker 镜像实际构建与 GitHub Actions 云端执行；配置已写好，本地 Node 构建已通过。
- 上游商业代理/转售条款确认、部署侧限流及并发限制。
- 官方对服务价值和收录的审核，以及活动是否接受测试网记录。
- GitHub PR、活动提交、EC 登记、奖励领取。

清单保持 draft，不含虚构公网地址或虚构交易哈希。

## Cloudflare 路线验收补齐清单

- [ ] 新增 Workers 入口及 Wrangler 配置，验证 Express/x402 在 Workers 运行时的兼容性。
- [ ] 在 Cloudflare 配置 PAY_TO、KITE_NETWORK=testnet、PRICE_USD，并核对与清单一致。
- [ ] 部署公开 HTTPS API，记录正式服务地址；接口必须允许 Agent 直接调用。
- [ ] 在该公网地址保存未付款 402 响应及 PAYMENT-REQUIRED，核对收款地址、金额、网络。
- [ ] 本人通过 Kite Passport 完成真实付费请求，保存 2xx、响应正文和结算交易哈希。
- [ ] 保存部署环境的失败不结算证据；既有模拟测试不能代替真实支付成功证据。
- [ ] 更新 service.yaml 的 pay_to、base_url；测试网部署且真实支付成功后才改 testnet，并重新校验。
- [ ] 确认 Google Public DNS 商业代理/转售条款、价格与可用性，记录来源。
- [ ] 确认主办方是否接受测试网支付记录；页面没有明确写可用测试网替代。
- [ ] 整理源码/PR、测试报告和活动提交材料，按活动流程跟进 EC 登记。

用户表示已填入钱包地址；尚未在本轮核对 .env 内容或同步到清单/平台。
Cloudflare 路线不要求构建 Docker 镜像；已有 Dockerfile 可保留为备选部署方式。
当前缺口主要是公网运行和本人真实付款闭环，不是再增加一份模拟测试报告。

### Workers 适配进展

- 已新增 `workers/index.js`、`wrangler.jsonc`、`cf:dev / cf:check / cf:deploy` 命令。
- 已安装 Wrangler 并锁定依赖；`.dev.vars`、`.wrangler` 加入忽略规则。
- 本地 `.env` 的 PAY_TO 地址格式有效，KITE_NETWORK 为 testnet。未打印地址或上传配置；格式有效不等于已确认钱包归属。
- 适配后重新检查：15 项测试、类型检查和 Node 构建均通过。
- esbuild-wasm 静态打包检查通过，不代表 Workers 运行验证通过。
- 阻塞：本机原生 esbuild 遭 SIGKILL；Wrangler 无法加载 workerd；Docker daemon 未运行。
- 尚未取得可靠 Cloudflare 登录状态，未上传或部署。下一步需恢复原生工具运行，或在 Linux/Docker 环境运行 Wrangler 验证。

### 容器验证更新

已通过 Docker Desktop + Linux Node 22 容器恢复 Wrangler：
- Dockerfile.tools 安装系统 ca-certificates，保持 HTTPS 验证开启。
- Wrangler deploy --dry-run 通过（未上传）。
- Workers 本地 HTTP 检查全部通过，详见 evidence/workers-unpaid-smoke.json。
- 修正付款中间件为首次合法付费路由请求时初始化，避免健康请求启动后台网络初始化。
- 修改后 15 项测试、类型检查、构建通过。
- Cloudflare whoami 确认旧登录已失效且不能刷新；需要用户重新 OAuth 授权。
- 仍未部署、未付费。以上替代先前“Docker 未运行、Workers 无法验证”的阻塞状态。

### 公网部署完成

- 地址：https://kite-live-dns.sakurasoga7.workers.dev
- Cloudflare 版本：5933b571-d0c9-4fe2-9859-bd1a02036b0c
- Kite 测试网，单次价格配置为 0.001 USD。
- 公网健康检查、402、参数/方法/路由检查通过，挑战收款地址与用户配置一致。
- 已更新清单的真实收款地址和 base_url。
- 仍为 draft：未执行本人真实付费调用，不能宣称已完成链上验收。
- 后续需取得成功付费 2xx 与交易哈希，并确认测试网证据是否符合活动要求。

### Passport 付款准备受阻

- 已完成用户邮箱登录，JWT 有效；未保存验证码或令牌到仓库。
- kpass 1.10.1 执行 sandbox on 成功，但 status 仍显示 passport.prod.gokite.ai。
- faucet drop --token pieUSD 被 CLI 拒绝：生产后端禁用水龙头。
- 钱包余额返回多链资产，但未返回 Kite 测试网 pieUSD。
- 原服务仓库的 sandbox/pieUSD 指南与当前 Passport 行为存在不一致，需确认支持 eip155:2368 的官方付款/水龙头入口。
- 未修改服务到主网，未充值，未创建付款会话，未执行付款。

### 付款兼容性进一步核对

实时检查确认 facilitator 支持 2368，测试 RPC 返回 chainId 2368。当前 Passport 文档的 dev 环境只列 Arc 测试网，与服务模板的 Kite pieUSD 流程不一致。已生成 官方确认问题.md，未对外发送。未更改收款链、未付款。

### 2026-09-18 PYUSD 真实付款验收通过

- 公网地址：`https://kite-live-dns.sakurasoga7.workers.dev`。
- 付款资产：Kite 测试网 PYUSD，合约 `0x8E04D099b1a8Dd20E6caD4b2Ab2B405B98242ec9`，单次 `0.01 PYUSD`。
- 已通过合约 `claim()` 领取 10 PYUSD；领取交易：`0x73190298a0b8ff84dc9d2389a919f5640e6de8df3241efd8be70c12760c48d60`。
- 真实付费请求返回 HTTP 200，并返回 `example.com` 的 A 记录。
- 付款交易：`0xf4c684514f7e55c278be613c9ecf911ad25e7e4bf8d8f1d2a7aab58f1fd84617`。
- RPC 独立核验：receipt `status=0x1`，区块 `22041152`，gas used `60892`。
- 服务端现在等待链上 receipt，只有 `status=0x1` 才返回 200，失败或回滚返回 402。
- 付款人与收款人为同一钱包，因此余额净变化为零；这不影响链上转账成功的验收结论。
- 完整结构化证据：`evidence/real-payment-2026-09-18.json`。
